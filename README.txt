Create the page with a 3x3 grid (css + bootstrap)

Add click listeners to squares to be able to make X or O (js)

Keep track of whos turn it is (js)
///
let bool = true
bool = !bool
///

function to check if game is over after each action (js)